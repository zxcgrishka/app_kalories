<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.Test2"
        tools:targetApi="31"
        android:usesCleartextTraffic="true">

        <!-- MainActivity пока что стартовая -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Base.Theme.Test2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- LoginActivity — обычная activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:theme="@style/Base.Theme.Test2" />

        <!-- RegisterActivity — для перехода из Login -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false"
            android:theme="@style/Base.Theme.Test2" />

        <activity
            android:name=".ui.home.AddDish.CreateDish.AddProduct.AddProductsActivity"
            android:exported="false" />

        <activity android:name=".CreateDishActivity"
            android:exported="false" />
        <activity android:name=".AddDishActivity"
            android:exported="false" />

        <!-- activity для камеры -->
        <activity
            android:name=".utils.CameraActivity"
            android:exported="false" />

    </application>
</manifest>

