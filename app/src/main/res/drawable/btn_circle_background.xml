<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- Заливка кнопки - измените цвет на нужный -->
    <solid android:color="@color/c3" />

    <!-- Обводка -->
    <stroke
        android:width="1dp"
        android:color="@color/c5" />

    <!-- Размер -->
    <size
        android:width="36dp"
        android:height="36dp" />
</shape>