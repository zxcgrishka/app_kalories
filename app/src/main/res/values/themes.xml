<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- БАЗОВАЯ тема (настройки) -->
    <style name="Base.Theme.Test2" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="dynamicColorThemeOverlay">@null</item>
        <item name="colorPrimary">@color/c1</item>
        <item name="colorPrimaryVariant">@color/c5</item>

        <!-- Для прозрачной навигации -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>

        <!-- Для прозрачного статус-бара -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>

</resources>